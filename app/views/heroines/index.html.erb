<h2>All Heroines</h2>

Search by Super Power <%= form_tag(heroines_path, :method => "get", id: "search-form") do %>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
<% end %>

<%if @heroines.blank? %>
  <h4>There are no Heroines with the Super Power <%= params[:search] %>.</h4>
<% else %>
<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name, heroine_path(heroine) %>
    </li>
    <br>
  <% end %>
</ul>
<% end %>
<p><%= link_to "Create new Heroine", new_heroine_path %></p>
